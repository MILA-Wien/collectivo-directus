{
  "name": "mono2",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "docker compose up -d; pnpm run -r --parallel dev",
    "test": "echo \"Error: no test specified\" && exit 1",
    "proxy": "lcp --proxyUrl http://localhost:8055 --credentials --origin http://localhost:3000"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
